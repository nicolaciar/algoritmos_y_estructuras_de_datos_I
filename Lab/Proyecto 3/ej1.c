#include <stdio.h>

int main () {
    // Estado A
    // int x = 7, y=3, z=5;
    // Estado B
    int x=1, y=10, z=8;
    
    // Expresiones
    printf("%d\n", x + y + 1);
    printf("%d\n", z * z + y * 45 - 15 * x);
   // exp3 = y - 2 == (x * 3 + 1) % 5;
   // exp4 = y / 2 * x;
   // exp5 = y < x * z;
}
